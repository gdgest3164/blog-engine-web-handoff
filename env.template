# Blog Engine Web — 환경변수 템플릿
# 사용자 → 다른 AI 웹 개발자에게 별도 전달 (이 값 채워서)
#
# 보안: service_role key 는 owner BFF 서버 측에서만 사용. 클라이언트 번들에 포함 금지.

# ============ Supabase ============
NEXT_PUBLIC_SUPABASE_URL=https://prraghboqtfefanlnlcj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<발급해서 채우기>

# 서버 측만 — 클라이언트 노출 금지
SUPABASE_SERVICE_ROLE_KEY=<발급해서 채우기, 서버 .env 만>

# ============ admin-api (백엔드 - Wave 4 이후 활성화) ============
ADMIN_API_BASE=https://api.virin.kr
# 또는 로컬 개발: http://localhost:7317

# ============ 검증 대상 URL ============
TARGET_URL=http://localhost:3000
# 스테이징: https://stage.virin-engine.kr

# ============ 테스트 계정 (Playwright E2E 용) ============
OWNER_TEST_USER_EMAIL=
OWNER_TEST_USER_PASSWORD=
CLIENT_TEST_USER_EMAIL=
CLIENT_TEST_USER_PASSWORD=

# ============ 시각 회귀 baseline 보관 위치 (선택) ============
VISUAL_BASELINE_DIR=./reports/visual-baseline
